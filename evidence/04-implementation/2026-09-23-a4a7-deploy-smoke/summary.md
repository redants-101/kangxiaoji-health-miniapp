# A3 修复 + A4–A7 测试环境部署与双手机验收准备 · 总结 — 2026-09-23

## 交付物索引（本目录）

| # | 交付 | 文件 |
|---|---|---|
| 1 | 部署记录 | deploy-record.md |
| 2 | 回滚快照记录 | deploy-record.md §2（v4 backup-a4a7-predeploy-20260923，10:51:33） |
| 3 | 部署后冒烟记录 | smoke-record.md + smoke-s*.txt（10/10 通过） |
| 4 | 测试结果与真实退出码 | target-tests.txt(0) / full-test.txt(0) / regression.txt(0) / lint.txt(1=基线 11e/32w，本批新增 0) |
| 5 | 体验版上传手册 | upload-manual.md（CLI 尝试 1 次失败，转人工） |
| 6 | 双手机验收手册 | dual-phone-manual.md（11 步） |
| 7 | 截图清单 | screenshot-list.md（A3-F01..05 / A4-F01..05 / A6-P01..08 / A7-T01..05；A2-S01/S06 不补拍） |
| 8 | 云端仍未验证项目 | cloud-pending.md（7 项，含 app_configs 创建报告——未自行创建） |
| 9 | 数据清理记录 | cleanup-record.md（4 合成文档 n=1 删除、复核 0；历史数据未动） |
| 10 | 结论 | 本文件 §结论 |

## 关键事实

- 部署前确认：$LATEST=A3 版（01:22:30）、路由空表（$LATEST 承接）、v1/v2/v3 在列——非凭历史记录假设，全部实时查询。
- 快照：v4（实际返回版本号）= A3 代码；回滚 `tcb fn config-route healthApi 4 100`。
- 部署：仅 healthApi，10:52:20，Nodejs16.13 未变，路由未改；线上代码含 3 个共享模块引用、A4/A6/A7 新路由、A3 装配修复注入。
- 冒烟：合成身份 10/10；零号码泄露（grep 0 命中）；未授权代录零写入（count=0）；A3 家属路由无 TypeError（修复运行时证明）。
- 本地：目标 93/93、全量 633/633（10:47 窗口外；窗口内 settings 时区对会失败 2 例，台账未伪装）、regression ok、lint 43=基线。
- 体验版：CLI 失败 1 次即停（未谎报成功）；**当前体验版仍为旧包 0.9.0**，人工上传 0.10.0 后前端才达验收态。
- 边界遵守：未正式发布、未部署 sendDueReminders、未改运行时/索引/安全规则、未删历史数据、未改真实健康数据、未创建 app_configs（已按约报告集合/字段/值/影响）、输出无密钥/完整手机号/openid/邀请码/真实健康数据。

## 结论

**可以开始双手机验收——但有一个前置未满足：体验版 0.10.0 需先按 upload-manual.md 人工上传并设置。** 云端侧已就绪（部署+冒烟通过）；上传完成并记录版本号/时间后，即可按 dual-phone-manual.md 执行 11 步验收与截图归档。

本轮停止于"部署完成 + 冒烟完成 + 体验版人工上传交接"。未进入 A8；不宣称真机验收完成；不宣称可正式上线（release 默认家庭 Tab 关，正式发布步骤未进入）。
