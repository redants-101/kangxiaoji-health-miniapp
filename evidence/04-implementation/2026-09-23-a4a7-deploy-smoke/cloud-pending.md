# 云端仍未验证项目清单（部署+冒烟后）— 2026-09-23

冒烟已证明（合成身份级）：邀请错误码四态、无绑定 familyView 拒绝、A3 路由无 TypeError（装配修复生效）、未授权代录拒绝+零写入、remind=false 不回传号码（完整/脱敏均不回）、appConfig 缺失保守回退、家庭页空态。以下为**冒烟未覆盖、需真机/真实双账号/后续授权才能证明**的项目：

1. **A4 写路径真实归属**：B 真机代录血压/血糖/代确认 → db 文档 `_openid=A` + 审计字段 + owner 端可见；代确认跨日界幂等（logId+confirmDate）；撤销代确认边界（本人/他人/重复）。
2. **A5 端到端**：A 撤销/改权后 B 真机下一次请求即拒、停留页不回填、旧请求晚归不覆盖（客户端机制已本地测试，真机时序未证）；B 端注销连带清理。
3. **A6 正向链路**：owner 真机配置真实测试号 → remind=true 家属脱敏展示 → 拨号至系统确认界面（不拨出）；清号/关 remind/撤销后取号即拒的真机时序。
4. **A7 远程开关**：`app_configs` 集合未创建（未获授权，未自行创建）。**报告**：如需验证远程覆盖，需控制台创建集合 `app_configs` + 文档 `{_id:'app_config', familyTabEnabled:false|true}` + 安全规则"仅云函数读写"；影响 = 仅此开关值，端侧缺省回退不受影响，不涉健康数据。创建后验证：改值 → 端侧 onShow 生效（trial 关→占位、release 开→真实页）。release 默认关的真机表现只能随正式包验收。
5. **日志级身份复核**：B 真机调用后立即 `tcb fn log healthApi` 取样，核对 `event.userInfo.openId` 与 family_members 行一致（掩码归档）——A4-0 遗留待办。
6. **体验版前端行为**：0.10.0 上传后全部页面级验收（当前云端已就绪、前端旧包）。
7. **频控/唯一索引真机并发**：邀请码频控 20 次/日与并发加入的唯一性（本地替身不能证明云端锁行为，历批台账延续）。
