# 待用户确认的环境与部署范围事项

以下事项无法从 CLI/代码侧确认，或属用户决策，**确认前不执行部署**：

## 1. 环境定位（必答）

账号下只有一个环境 `kangxiaoji-d5gw2k203f0488a9e`，**没有独立测试环境**。请确认：

- 该环境是否已绑定**已发布**的小程序（线上正式版是否在用它）？CLI 无法查证小程序发布状态与云环境绑定关系。
- 若已绑定线上：A0–A2 收尾版直接部署到该环境是否可接受？（兼容性分析见 deploy-scope.md §2：家庭 Tab 未开放，主要风险面不可达；但 family_auth 旧结构文档在新代码下按"缺失字段=false"处理，若有真实用户数据需先确认——当前实测 family_members 0 条、family_auth 13 条历史测试文档，decisions #7 记录"线上无真实家庭数据"，与该实测一致。）
- 若希望先建独立测试环境再部署：需用户在控制台新建环境并告知 envId（本轮不操作）。

## 2. 部署范围（必答）

- 仅部署 `healthApi`（sendDueReminders 零改动不部署）——是否同意？
- 部署前是否按 rollback-steps 方案 B 先发布 `backup-a2-deployed-20260922` 版本快照（推荐）？

## 3. 历史测试数据（可延后）

family_auth 现存 13 条历史测试文档（11 条 pending 带旧邀请码、2 条已清码）。新代码下旧 pending 码仍可被查询/加入（未过期者）。请确认：

- 是否在部署前/后清理（删除或置 revoked）？本轮未动。若清理，建议保留 1-2 条供 S02 正常预览截图使用（或截图前新生成）。

## 4. 小程序前端上传（可延后）

前端改动（noInvite 页面态、错误态面板、三开关授权页等）需微信开发者工具上传（体验版或正式提审）才对真机生效。开发者工具本地编译联调**不需要**上传。请确认联调方式（见 screenshot-manual §0），据此决定何时上传。

## 5. git 入库时机（提示）

三个共享模块（family-policy/payload-helpers/payload-validation）与全部批次改动仍未提交。**从本机磁盘部署没有问题**；但在提交入库前，禁止从 git 检出/CI 部署（会缺未跟踪文件导致函数崩溃）。是否在本轮之后安排一次提交，由用户决定（此前各批次均按"不提交"指令执行）。
